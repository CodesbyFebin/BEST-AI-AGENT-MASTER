import { SITE } from "@/lib/site";import { xmlResponse } from "@/lib/xml";import { entities } from "@/lib/catalog";export const dynamic="force-static";export async function GET(){const verifiedServerUrls=entities.filter((e)=>e.type==="mcp-server"&&e.verification==="verified").map((e)=>`${SITE.url}/mcp/servers/${e.slug}`);return xmlResponse([`${SITE.url}/mcp`,`${SITE.url}/mcp/servers`,...verifiedServerUrls])}
